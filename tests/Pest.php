<?php

use Illuminate\Foundation\Testing\RefreshDatabase;
use Tests\TestCase;

uses(TestCase::class)->in('Feature');

// You can add more global helpers or configurations here
// For example, if you want to use RefreshDatabase trait globally for feature tests:
// uses(RefreshDatabase::class)->in('Feature');
